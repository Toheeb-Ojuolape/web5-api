import type { DataStore } from './data-store.js';
import type { MessageStore } from './message-store.js';
import type { BaseMessage, Filter } from '../core/types.js';
import type { RecordsWriteMessage } from '../index.js';
/**
 * A class that provides an abstraction for the usage of MessageStore, DataStore, and EventLog.
 */
export declare class StorageController {
    static query(messageStore: MessageStore, dataStore: DataStore, tenant: string, filter: Filter): Promise<RecordsWriteMessageWithOptionalEncodedData[]>;
    static delete(messageStore: MessageStore, dataStore: DataStore, tenant: string, message: BaseMessage): Promise<void>;
}
export declare type RecordsWriteMessageWithOptionalEncodedData = RecordsWriteMessage & {
    encodedData?: string;
};
//# sourceMappingURL=storage-controller.d.ts.map