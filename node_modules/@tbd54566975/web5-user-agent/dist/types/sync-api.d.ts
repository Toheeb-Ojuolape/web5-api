import type { DidResolver } from '@tbd54566975/dids';
import type { Dwn, Event } from '@tbd54566975/dwn-sdk-js';
import type { ProfileManager } from './profile-manager.js';
import { SyncManager } from './sync-manager.js';
export type SyncApiOptions = {
    dwn: Dwn;
    didResolver: DidResolver;
    profileManager: ProfileManager;
    storeLocation?: string;
};
type Direction = 'push' | 'pull';
export declare class SyncApi implements SyncManager {
    #private;
    constructor(options: SyncApiOptions);
    clear(): Promise<void>;
    registerProfile(did: string): Promise<void>;
    enqueuePush(): Promise<void>;
    getEvents(did: string, watermark: string | undefined, dwnUrl: string): Promise<Event[]>;
    push(): Promise<void>;
    enqueuePull(): Promise<void>;
    pull(): Promise<void>;
    getWatermark(did: string, dwnUrl: string, direction: Direction): Promise<string>;
    setWatermark(did: string, dwnUrl: string, direction: Direction, watermark: string): Promise<void>;
}
export {};
//# sourceMappingURL=sync-api.d.ts.map